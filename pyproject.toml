[project]
name = "mcp-remort-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.12.4",
    "aiosqlite>=0.19.0",
    "aiofiles>=24.1.0",
]

[tool.poetry.dependencies]
python = "^3.11"
